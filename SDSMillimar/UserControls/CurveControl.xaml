<UserControl
    x:Class="SDSMillimar.UserControls.CurveControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SDSMillimar.UserControls"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="White"
    mc:Ignorable="d">
    <Grid Background="#095fa8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="16"
            FontWeight="Bold"
            Foreground="White"
            Text="数据曲线" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <Border Background="White">
                <lvc:CartesianChart Margin="10" Series="{Binding SeriesData, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <!--  定义多个坐标轴  -->
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            Title="直径"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="Black"
                            MinValue="0" />
                        <lvc:Axis
                            Title="圆度"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="Black"
                            MinValue="0" />
                        <lvc:Axis
                            Title="跳动"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="Black"
                            MinValue="0" />
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            Title="秒"
                            MaxValue="{Binding XMax}"
                            MinValue="{Binding XMin}">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </Border>
            <!--  自定义 Legend  -->

            <ItemsControl
                Grid.Column="1"
                Background="White"
                ItemsSource="{Binding LegendItems, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Margin="5"
                            HorizontalAlignment="Left"
                            Orientation="Horizontal">
                            <!--  点击颜色块显示/隐藏折线  -->
                            <!--<CheckBox HorizontalAlignment="Center" IsChecked="{Binding Visibility}" IsEnabled="False" VerticalAlignment="Center" Margin="0,0,5,0"/>-->
                            <Border
                                Width="20"
                                Height="10"
                                Background="{Binding Color}"
                                Cursor="Hand"
                                MouseLeftButtonDown="Border_MouseLeftButtonDown" />
                            <TextBlock
                                Margin="3,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="12"
                                Text="{Binding Title}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

    </Grid>
</UserControl>

